div(ng-include='"components/navbar/navbar.html"')
.container.with-nav
  .row
    //- .col-sm-8.col-sm-offset-2
    //-   //- img.img-responsive(src="/assets/images/jane.jpg")
    //-   div.well
    //-     p.
    //-       We created this website because we wanted a private way to share
    //-       our daughters life with those we know and love. In order to make sure that you are actually
    //-       someone we love (or at least someone that we know), please create an account and we'll let you know once we've 
    //-       confirmed your identity.

    .col-sm-12.col-md-8.col-md-offset-2
      h1 Login
    .col-sm-12.col-md-8.col-md-offset-2
      form.form(name='form', ng-submit='login(form)', novalidate='')
        .form-group
          label Email
          input.form-control(type='email', name='email', ng-model='user.email')
        .form-group
          label Password
          input.form-control(type='password', name='password', ng-model='user.password')

        .form-group.has-error
          p.help-block(ng-show='form.email.$error.required && form.password.$error.required && submitted')
            | Please enter your email and password.
          p.help-block {{ errors.other }}

        div
          button.btn.btn-primary.btn-lg.btn-login(type='submit')
            | Login
          = ' '
          a.btn.btn-default.btn-lg.btn-register(href='/signup')
            | Sign Up


  hr
